for ({{ Initialization }}{{ if !Initialization }}{{ end }}{{ if Condition }} {{ Condition }}{{ end }}; {{ if Iterator }}{{ Iterator }}{{ end }}) {
{{~ for statement in Body ~}}
    {{ statement }}
{{~ end ~}}
}

{{~ if ErrorName ~}}
{{~ if Arguments.size > 0 ~}}
revert {{ ErrorName }}({{ Arguments | array.join ", " }});
{{~ else ~}}
revert {{ ErrorName }}();
{{~ end ~}}
{{~ else ~}}
revert({{ if Message }}"{{ Message }}"{{ end }});
{{~ end ~}}

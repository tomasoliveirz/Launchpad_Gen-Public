{{~ for block in ConditionalBlocks ~}}
{{~ if for.first ~}}

    if ({{block.Comparison}}) {
{{~ else if block.IsElseBlock ~}}
    else {
{{~ else ~}}
    else if ({{block.Comparison}}) {
{{~ end ~}}
{{~ for statement in block.Statements ~}}
        {{statement}}
{{~ end ~}}
    }
{{~ end ~}}